#Spring R2DBC-mysql for reactive data base for mysql
#no try catch can be used

#Mono error(new SomeException)
.onErrorReturn()
.onErrorResume(ex->)
.onErrorMaP(ex -> othe Exp)

# .doFinally( signtyp)